{
  "name": "zerodha-kite-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "Read-only MCP server for Zerodha Kite Connect portfolio and quote data.",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "dashboard": "npm run build:css && node src/web-server.js",
    "build:css": "sass src/styles/app.scss src/styles/.app.compiled.css --no-source-map && tailwindcss -i src/styles/.app.compiled.css -o public/styles.css",
    "watch:css": "sass src/styles/app.scss src/styles/.app.compiled.css --no-source-map --watch",
    "smoke": "node scripts/smoke.js"
  },
  "engines": {
    "node": ">=24"
  },
  "devDependencies": {
    "sass": "^1.83.4",
    "tailwindcss": "^3.4.17"
  }
}
